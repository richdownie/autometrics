<h1>Listing iterations</h1>

<table>
  <tr>
    <th>Title</th>
      <th>Default</th>
  </tr>

<% @iterations.each do |iteration| %>
  <tr>
    <td><%=h iteration.title %></td>
      <td><%=h iteration.default %></td>
    <td><%= link_to 'Edit', edit_iteration_path(iteration) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New iteration', new_iteration_path %> | <br/>
<%= link_to 'Back to Manual Steps', manuals_path %>