<h1>Watircuke Results</h1>

<table>
  <tr>
    <th>Title</th>
  </tr>

<% @results.each do |result| %>
  <tr>
    <td><%=h result.title %></td>
    <td><%=h result.created_at %></td>
  </tr>
<% end %>
</table>
