<%= link_to 'ADD SCENARIO', new_manual_path %> | <%= link_to 'Results', results_path %>
<br/>
<br/>
<% @manuals.each do |manual| %>
  <%= link_to(manual.scenario, edit_manual_path(manual)) %>
  <%= link_to 'X', manual, :confirm => 'Are you sure?', :method => :delete %><br/>
<% end %>