<script>
$("span#add-button").mouseenter(function () {
$("span#add").show();
});
$("span#add-button").mouseleave(function () {
$("span#add").hide();
});
$("span#reset-button").mouseenter(function () {
$("span#reset").show();
});
$("span#reset-button").mouseleave(function () {
$("span#reset").hide();
});
$("div#search").mouseenter(function () {
$("span#search-info").show();
});
$("div#search").mouseleave(function () {
$("span#search-info").hide();
});
$("td.bug").hover(function(){$(this).fadeOut(100);$(this).fadeIn(100);});

$(document).ready(function() {
    if ($("input#hide_pass").is(":checked"))
  {
    $("div#pass").show("fast");
  }
    else
  { 
    $("div#pass").hide("fast");
  }
  });

$(document).ready(function() {
    if ($("input#hide_fail").is(":checked"))
  {
    $("div#fail").show("fast");
  }
    else
  { 
    $("div#fail").hide("fast");
  }
  });

$(document).ready(function() {
    if ($("input#hide_block").is(":checked"))
  {
    $("div#blocked").show("fast");
  }
    else
  { 
    $("div#blocked").hide("fast");
  }
  });

$(document).ready(function() {
    if ($("input#hide_untested").is(":checked"))
  {
    $("div#untested").show("fast");
  }
    else
  { 
    $("div#untested").hide("fast");
  }
  });


$("input#hide_pass").click(function (){

  if ($("input#hide_pass").is(":checked"))
{
  $("div#pass").show("fast");
  $("input#hide_fail").attr('checked', false);
  $("input#hide_block").attr('checked', false);
  $("input#hide_untested").attr('checked', false);
  $("div#fail").hide("fast")
  $("div#blocked").hide("fast");
  $("div#untested").hide("fast");
}
  else
{ 
    $("div#pass").hide("fast");
}
});


$("input#hide_fail").click(function (){

  if ($("input#hide_fail").is(":checked"))
{
  $("div#fail").show("fast");
  $("input#hide_pass").attr('checked', false);
  $("input#hide_block").attr('checked', false);
  $("input#hide_untested").attr('checked', false);
  $("div#pass").hide("fast");
  $("div#blocked").hide("fast");
  $("div#untested").hide("fast");
}
  else
{ 
    $("div#fail").hide("fast");
}
});

$("input#hide_block").click(function (){

  if ($("input#hide_block").is(":checked"))
{
  $("div#blocked").show("fast");
  $("input#hide_pass").attr('checked', false);
  $("input#hide_fail").attr('checked', false);
  $("input#hide_untested").attr('checked', false);
  $("div#pass").hide("fast");
  $("div#fail").hide("fast");
  $("div#untested").hide("fast");
}
  else
{ 
    $("div#blocked").hide("fast");
}
});

$("input#hide_untested").click(function (){

  if ($("input#hide_untested").is(":checked"))
{
  $("div#untested").show("fast");
  $("input#hide_pass").attr('checked', false);
  $("input#hide_fail").attr('checked', false);
  $("input#hide_block").attr('checked', false);
  $("div#pass").hide("fast");
  $("div#fail").hide("fast");
  $("div#blocked").hide("fast");
}
  else
{ 
    $("div#untested").hide("fast");
}
});
</script>